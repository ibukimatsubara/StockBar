# StockBar 要件定義

macOS のメニューバーに株価を表示する常駐アプリ。

## 1. コア表示機能

### 1.1 表示内容
登録銘柄ごとに、メニューバー上に以下を表示する：
- 銘柄名（または銘柄コード）
- 現在値（円）
- 当日の値幅（前日終値比、円 + パーセント）
- 騰落の色分け：
  - **プラス → 赤**
  - **マイナス → 緑**
  - （日本市場の慣習に合わせる）

表示フォーマット例：
```
日経平均 38,250 +120 (+0.31%)   ← 赤
トヨタ   2,845  -15 (-0.52%)    ← 緑
```

### 1.2 ローテーション表示
- 登録銘柄が複数ある場合、一定間隔で順に切り替えて表示する
- 表示順は後述の管理画面で並び替え可能
- 切り替え速度はユーザーが設定可能（例: 3秒 / 5秒 / 10秒）

### 1.3 自動更新
- 市場が開いている間は最新値を定期取得する
- 市場が閉じている間は更新を止めて最後の値を表示（負荷軽減のため）
- 更新間隔は設定可能（デフォルト: 1分など）
- 対応市場：少なくとも東証（日本株、9:00-11:30 / 12:30-15:00 JST）
  - 将来的に米国市場対応も視野

## 2. 銘柄管理 UI

メニューバーアイコンをクリックすると管理ダイアログ（ポップオーバー or ウィンドウ）が開く。

### 2.1 銘柄リスト
登録銘柄を一覧表示。各行に：
- 銘柄コード（例: `7203.T`、`^N225`）
- 銘柄名
- 表示/非表示トグル
- 並び替えハンドル（ドラッグで順序変更）
- 削除ボタン

### 2.2 銘柄の追加
- 「追加」ボタン → 銘柄コード入力フィールド
- コードを入力 → API で名称を取得して確認 → 登録

### 2.3 表示制御
- **個別の非表示トグル**：特定銘柄をローテーションから一時的に外す
- **一括非表示（"集中モード" / "会議モード"）**：
  - メニューから1クリックで全銘柄を隠す（アイコンだけ残す or ダミー文言）
  - 先方との定例中など、画面共有時に株価を見せたくないシーン用
  - もう一度クリックで復帰

### 2.4 設定項目
- ローテーション速度
- 更新間隔
- 表示フォーマット（コード or 銘柄名、パーセント表示有無 など）
- 色の反転オプション（赤緑が逆の文化圏向け、必要なら）

## 3. データソース

候補（要選定）：
- Yahoo Finance 非公式 API … 無料・銘柄数豊富だが非公式
- Alpha Vantage … 公式・無料枠あり、レート制限きつめ
- Finnhub … 公式・無料枠あり
- stooq.com … CSV で取得可、日本株も対応

**当面は Yahoo Finance（`query1.finance.yahoo.com`）を使う想定**で進める。
銘柄コードは Yahoo 形式（例: `7203.T`、`^N225`、`AAPL`）。

## 4. データ永続化

- 登録銘柄リスト・設定値は `UserDefaults` または JSON ファイルに保存
- 起動時に復元

## 5. 技術スタック

- Swift + SwiftUI
- `MenuBarExtra`（macOS 13+）
- ネットワーク: `URLSession` + `async/await`
- 配布: 当面はローカルビルド、将来的に署名/公証を検討

## 6. スコープ外（将来検討）

- アラート通知（指値到達時にプッシュ）
- チャート表示
- ポートフォリオ管理（保有数・評価損益）
- 複数通貨対応
- ウィジェット化

---

## 開発フェーズ案

1. **Phase 1**: 1銘柄固定で取得 → メニューバーに表示する最小実装
2. **Phase 2**: 複数銘柄 + ローテーション
3. **Phase 3**: 銘柄管理ダイアログ（追加/削除/並び替え/非表示）
4. **Phase 4**: 設定（速度・更新間隔）+ 集中モード
5. **Phase 5**: 永続化・市場時間判定・仕上げ
